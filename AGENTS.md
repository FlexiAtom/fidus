# fidus 项目约定

> 这份文件是**被坑出来的**。每一条都对应一次实际踩坑或一次审查发现的真实缺陷。
> 新增约定时请附上"因为什么事故而加"。

## 0. 最高准绳

附录 A 的五条设计原则（见 `docs/spec.md`）优先级高于任何功能。
**任何新增功能若与之冲突，改功能，不改原则。**

零信任不是一句口号，也不是"兜底时的无奈"——它是被平台 API 坑出来的**被迫且唯一**的选择。
因此：**能用类型封死的，绝不靠约定和注释封。**

## 1. 失效模式注释（Failure-Mode Comment）

**规则**：任何"细化""优化""放宽""特例放行"类的代码，注释里**必须**写明：

1. **为什么这样做**（收益）
2. **什么条件下它会崩**（失效模式）
3. **为什么那个条件不可达 / 可达时会发生什么**（兜底）

只写第 1 条的注释视为不完整。

**因为什么事故而加**：2026-09 的三方审查发现 9 条 Critical，其中 5 条的共同特征是
**注释声称的行为与实际行为不符**：

| 注释声称 | 实际行为 |
|---|---|
| `AREA_MAX_FACTOR` 能拒绝"离场+到达合并"的 blob | 只对位移 > 半个目标宽成立，小位移全部漏网 |
| MotionGate 按 500ms 窗口判决 | `last_reference` 只写不读，一切按**调用次数**计 |
| L1 phase 2 做全分辨率细化 | 粗/细分数不可比，细化分支常年为假，等于空转 |
| 类型层面不存在坐标输入入口 | `AffineTransform` 系数 pub + `CoordinateFrame::new` pub，可随手注入 |
| baseline 慢升可安全吸收噪声静态区 | 22 次调用后真动画被放行，且 threshold 会被推过 1.0 永久失效 |

这 5 条如果当初写了失效模式，至少 3 条在**写的时候**就会被自己发现。

注释啰嗦不要紧，再踩一次才要命。

## 2. 数值/状态机代码必须做终局推演

写下任何带反馈的更新式（EMA、自适应阈值、协方差递推）后，**必须**推演 n→∞ 的终局：

- 这个量会收敛到哪里？
- 收敛后阈值/增益变成什么？
- 有没有单向棘轮（只升不降 / 只降不升）？
- 有没有"某状态一旦进入就再也出不来"的吸收态？

**因为什么事故而加**：MotionGate 的 baseline 非对称 EMA 在 R>1/3 的稳定动画下会把
threshold 推过 1.0，而 ratio ∈ [0,1] 永远无法超过 → 门控永久失效、无回落路径。
写的时候只想了"慢升所以安全"，没算终局。

推荐做法：写完就用 20 行 Python 跑一遍 n=1..200，把关键量打出来看。

## 3. 就地更新的顺序

递推公式里如果同一个变量既是输入又是输出，**先存旧值**，不要就地覆盖后继续用。

**因为什么事故而加**：`kalman.rs` 的 `update()` 第 64 行把 `pv` 覆盖成 `pv*(1-kp)`，
第 65 行的 `vv -= kv*self.pv` 就用到了新值——实际减去的量比应减量小 **26 倍**，
速度方差被系统性高估 8%。

优先使用 Joseph 形式等**数值稳定且不依赖书写顺序**的形式。

## 4. 防除零不等于拒绝

`.max(1e-6)` / `.clamp()` 这类"防除零"会把**退化输入伪装成合法输出**。

退化就该 `return None` / `Err`，不要兜底成一个看起来正常的数。

**因为什么事故而加**：`template.rs` 的 NCC 分母 `.max(1e-6)`，在纯色窗口
（den_f≈1e-8）下让量化噪声除出 |score|≈1 的**满分假匹配**，直接喂进 Kalman。
这正是原则 4"概率池只接收真实测量"要防的东西。

## 5. 测试可能固化 bug

写断言前先问：**这个"期望行为"是规格要求的，还是当前实现碰巧的产物？**

**因为什么事故而加**：`motion_gate.rs` 的 `armed` 字段写了但从不参与判决，
导致"冷启动第一帧必不通过"——而规格的 2 帧规则只针对"动态结束后"，冷启动不该罚。
这个错误行为被测试 `static_then_dynamic_then_resume` 的第一行断言**固化**了下来。

规格有明文的，注释里引用条款号（如 `spec §6.1`）。

## 6. 实机踩坑固化为仿真回归测试

每个在真实合成器上踩到的坑，都要变成一个**不需要显示服务器**就能跑的仿真测试。
`fidus-calibrate/tests/sim.rs`（L9）与 `anchor_sim.rs`（L0）是范例：
分数缩放、面板偏移、动态壁纸噪声、光标穿透、歧义拒绝、盲投影、teardown 路径。

审查发现的测试盲区也照此补：小位移、贴边目标、高频调用、长时间饱和。

## 7. 参数不要写死在函数体里

公开的可调字段如果没被实现读取，就是**骗人的旋钮**。

**因为什么事故而加**：`fused.rs` 的 `r_min_px2` / `r_max_px2` 是 pub 字段，
但 `r_of()` 里硬编码 `1.0 + (1-c)*399.0`，调参完全无效。

## 8. 判决用的量，必须验证它是你关心的属性的代理

给任何"质量/合法性"设阈值前，先用 20 行脚本测一遍：
**这个指标与我真正在意的属性，是不是单调相关？**

尤其警惕"典型情况下对、边界情况下反着"的指标。

**因为什么事故而加**：P2-f 一次性发现三条同型缺陷（详见规格 §11）：

| 用来判决的量 | 以为它代表 | 实测 |
|---|---|---|
| 模板方差 | 可定位性 | **毫无分辨力**：线性渐变 std=73.6 自相似恒 1.000（无法定位），哈希纹理 std=74.8 自相似 0.025（最佳）——方差几乎相同，结论截然相反 |
| blob 面积 | 能否识别"离场+到达合并" | 原理上分不开：2px 位移的合并 blob 面积 ≈ 静止 blob |
| 离预测的距离 | 是否发生了位移 | 慢速漂移 2.83px 判为"原地"，但重绘了 92% 像素 |

三条的后果都不是报错，而是**静默产出看起来合理的值**——渐变模板会以满置信度
把 Kalman 拽到虚构位置，且永不自我暴露。这类缺陷必须按 Critical 处理。

判错的后果是"报错"还是"看似合理的值"，是判断严重性的第一问。

## 9. 工程约定

- 提交信息：英文，带里程碑前缀（`P2-f: ...`），正文说明**为什么**而非只说改了什么。
- 文档与注释：面向读者的文档中文，代码内注释英文（与现有代码一致）。
- `cargo clippy --workspace --all-targets` 必须零警告。
- `cargo test --workspace` 必须全绿。
- `cargo doc --workspace --no-deps` 必须零警告（**因为什么事故而加**：P2-i 时发现 12 条 rustdoc 警告已静默堆积——失效的文档内链接、指向私有项的公开文档。clippy 和测试都不查这些，但读文档的人会撞上）。
- 后端只适配"投射 + 截屏"两个原语，**永不**绑定任何报告窗口几何的协议。
- 新定位思路通常走 **提案 → 自审裁枝 → 草案 → 方案** 四步，阶段含义必须严格区分：**提案只是一个可能实现的想法**，回答是否值得继续研究，不承诺可行，也不授权实现；**草案是一个可行的想法**，至少包含简单实现细节、限定条件、主要失效模式和验证方向，但仍不是最终程序定义；**方案是足以限定实现后程序逻辑的完整描述**，实现者只要遵守方案，最终程序的逻辑行为就不得偏离方案。提案、草案和方案均先经过相应审阅，不得用后阶段措辞倒灌前阶段结论。
- 自审裁枝不是批准实现，而是依据实际代码、失效模式和收益成本，删除不成立、重复或证据不足的候选，只把保留下来的范围带入草案；当方案细节不足以支撑实际行动时，必须增加 **计划书** 阶段。
- 方案或计划书进入执行前，必须经过**可执行性评估**：确认目标、代码入口、依赖、顺序、验收标准、回滚边界和授权条件足够明确；评估未通过不得实现。

## 10. 状态、审查、批准与证据语义

以下术语按语境使用，不互相替代：

- **全量审查**：在其声明的审查范围内检查是否还有明显更优雅的替代方案，是否还有明显不合理、可剪掉或应修正的设计，以及**是否存在逻辑问题**；若没有，审查记录应明确写出“未发现明显更优雅替代方案，未发现可继续剪掉的不合理设计，未发现逻辑问题”。“有条件通过”不是项目固有状态，而是审查者针对仍有明确条件时使用的结论词；条件必须逐项列出，不能只写“有条件”。
- **人工审阅**：人工阅读对应阶段文档，认为在该阶段语境下没有发现问题。它不自动授权实现，也不代表实现完成；是否授权取决于该语境的明确规则。
- **人工批准**：人工审阅“提案、草案或方案”文档后的特指批准。它只允许进入该文档规定的下一阶段，不代表实现完成，不替代真实实验、发布或 push 授权。
- **明确授权**：泛指对具体动作的授权，包括真实桌面 mutation、破坏性实验、签名、发布或 push；人工批准是其中针对提案/草案/方案文档的一个特定变体。
- **方案冻结**：已实现大部分的方案被发现无法完整实现或不值得继续投入，但已有实现可能生效或确实存在能生效的部分；因此停止继续扩展并保留现状。它不是完成、通过或失败。若只冻结设计、执行或结论，必须明确写出冻结对象。
- **自审通过并转入方案**：自我审查后没有发现明显需要剪枝或更优雅替代方案，且设计逻辑没有发现问题；它只表示可以转入方案，不表示实现完成。
- **社区互助处理中**：人工进行外部对接；本项目语境下，表示由用户转达他人进度或证据。它是挂起的变体，不是通过、完成或发布状态；收到信息后仍须按证据等级重新审查。
- **继续**：只有在 Agent 因意外情况中断作业后，用户说“继续”才是合法的恢复指令；其他语境下的“继续”不是实现、授权或阶段推进指令，必须要求用户明确动作。

### 证据等级

固定使用以下证据层级，并在结论中标明等级：

```text
L0 静态检查：diff、shell 语法、Python 语法等
L1 单元/纯函数测试：不连接真实显示环境
L2 fake/model/protocol fixture：验证状态机和错误语义，不证明真实 compositor
L3 本机真实 host：只证明当前 revision、机器和 compositor
L4 独立机器：证明指定 revision 在另一环境的复现情况
L5 发布证据：clean source、artifact、SBOM、provenance、signature 一致
```

低等级证据不得升级为高等级结论：fake pass 不是真实 compositor pass，本机 pass 不是跨机器 pass，字段 read-back 相等也不是 stable identity confirmed。

### 完成与任务分组

“已完成”必须绑定完成定义、命令和证据等级；否则只能写“部分实现”。任务表至少分为：

1. **核心目标**：自身窗口定位、校准、视觉跟踪和失败时不伪造位置；
2. **安全与完整性**：输入边界、生命周期、恢复语义、协议解析和默认路径不 mutation；
3. **发布与扩展**：跨机器、签名、SBOM、Docker release、新平台 backend 和真实异常实验。

评估时先判断核心目标，再分别报告安全与完整性、发布与扩展，不得将三类任务混成一个完成度数字。

## 11. 提案必须先验算核心假设

**提案（`docs/proposals/`）里的每一条可验证声称，都要在提案阶段就实测。**
不能写"需实机验证（未承诺可用）"然后把验证推给实现阶段——那正是返工的来源。

一份合格的提案至少回答：

1. **核心假设成立吗？** 用 20–50 行脚本实测，把数字贴进提案。
2. **这个方案解决谁的问题？** 现有方案在什么场景下真的不够用？答不出就不该实现。
3. **方案内部自洽吗？** 各个组成部分是否互相拆台。

**因为什么事故而加**：P3/L10 的提案阶段实测发现，规格 §4.2 传承自 v0.4 的
"对数螺旋网格 + 锁定 FFT 主频"**自相矛盾**——对数螺旋消除整周期歧义的代价，
恰恰是摧毁"主频"这个概念本身（峰锐度 3.6 vs 规则光栅 250.9）。而规则光栅
虽有 0.03px 的相位精度，却只能确定 `offset mod period`，无法独立定位。

这个设计从早期 v0.4 草案一路“抢救”进 v0.5.1 规格，**在这段不足三周的项目迭代中一直没人验算过**。
几十行 numpy 就能问出来的问题，代价却可能是一整个里程碑的返工。

同时它还纠正了一条**写反了的风险**：原文担心"动态壁纸频谱污染"，
实测窄带相位估计对宽带噪声天然免疫（强纹理壁纸下误差仍仅 0.06px）。
**担心错了对象，比不担心更危险**——它会把注意力从真正的障碍上引开。

> 提案的价值在于**它可以得出"不做"的结论**。拦下一个原理上不成立的方案，
> 代价是几十行脚本和一次审阅；放它进实现，代价是一个里程碑。

## 11. 实机验证必须假设「人正在用这台电脑」

**基准场景是有人在办公的真实桌面**：终端在滚动、光标在闪烁、聊天窗口在刷新、
浏览器在跑动画。**不是一块静止的干净屏幕。**

因此：

- 任何"实机验证通过"的结论，**必须在屏幕有正常活动时取得**。安静桌面下的
  0 失败不构成证据，只说明当时没人用电脑。
- 写"背景静止"这类前提时，**必须把它当作已知会被违反的前提**去设计，
  而不是当作环境保证。
- 测量脚本**自己的输出也是屏幕内容**。把日志重定向到文件再读，否则测的是
  自己打印的文字（见下）。

**因为什么事故而加**：P3 追查 L9 间歇失败时踩了两次：

1. 我把失败归因于分数缩放，因为"scale=1 下测出 0/20"——实际那批测试期间
   屏幕恰好安静。换个时间重测，scale=1 同样失败 7/15。**成因与缩放无关。**
2. 我曾测到"分数缩放下背景差分 30361 像素"并写进测量文档当核心证据。
   那是**探测进程启动时我自己的终端在滚动**。同进程内连采后，任何 scale
   下静止背景都逐像素恒定。

真正的成因是差分检测隐含了"标记是两帧间唯一的变化"这个**从未写下来的前提**，
而真实桌面上别的窗口会自发重绘——29 对相邻帧里有 1 对整屏变了 144386 像素。
单次校准采约 28 帧，撞上的概率 `1-(1-1/29)^28 = 63%`，与实测失败率吻合。

> 这个 bug 是被"用户当时正在用电脑"这个巧合提前暴露的。
> **把真实场景当默认，才能让这类巧合变成常态而不是运气。**

## 12. 完成度评估与评估后剪枝

项目完成度必须围绕已定义的核心目标评估：**fidus 是否足够定位调用方自身窗口**。
这里的“自身窗口”是调用方能够提供离屏渲染模板的窗口；不把定位任意第三方窗口、窗口枚举、窗口实例身份、多显示器通用几何、跨平台覆盖或正式发布，默认扩大为本项目的完成条件。

每次完成度评估必须先列出候选检查项，再做一次**评估后自我剪枝**：

1. 保留直接回答自身窗口定位目标、影响核心安全边界，或已被规范列为发布准入的检查项；
2. 删除与目标无关、重复已有证据、只服务未承诺能力，或没有明确决策用途的检查项；
3. 对保留项说明它影响的用户场景、所需证据和失败后决策；
4. 不得因为检查项越多就把项目完成度写得越低，也不得用与目标无关的未实现功能制造“未完成”；
5. 剪枝不等于隐瞒风险：被删除的事项若影响安全、数据完整性、已承诺支持范围或发布准入，必须转入“独立挂起事项”，并说明它不阻塞自身窗口定位目标的理由。

**因为什么事故而加**：此前全量评估把“任意窗口定位”、跨平台 backend、跨机器发布和真实 compositor 异常证据与“自身窗口是否可定位”混在一起，导致已完成的核心定位链路被无关缺口反复否定，也让真正影响目标的证据优先级不清。完成度评估必须先对齐目标，再剪枝不必要的评估，最后分别报告核心完成度与独立发布/扩展门槛。

## 13. 项目进程附件

项目当前正在进行的草案、已冻结方案、挂起事项和即将进行的任务，统一记录在 [`AGENTS.project-process.md`](AGENTS.project-process.md)。该附件不替代本文件的设计原则与工程约定；P5 完成后必须对整个项目执行一次**全量审查**，具体范围和准入条件以附件第 3 节为准。

## 13. 后续工作循环

所有非琐碎的后续工作必须按下面的循环推进，不得从“想做”直接跳到实现：

```text
可能的下一步
  → 提案
  → 自审裁枝
  → 草案
  → 方案
  → 可执行性评估 ──细节不足──→ 计划书 ──补足行动细节──┐
                         │                             │
                         └──────细节充分────────────────┘
                                          ↓
                         执行 → 全量审查（整体复盘）
                          ↑                  │
                          └──依据复盘结果继续┘
```

**计划书**不是新的设计层，也不是绕过方案审批的快捷方式；它是把已经裁决的方案拆成可执行工作包的行动文件。计划书至少明确：工作包、实际代码入口、依赖和顺序、每步验收命令、失败/回滚方式、需要的环境或人工授权，以及完成定义。

**因为什么事故而加**：此前“草案/方案 → 执行”之间缺少行动粒度评估，导致多个实际缺口同时暴露：P5 runner 的锁/进程清理、CI 离线依赖、发布清单和真实验证边界分散到不同轮次才被发现。方案方向可能正确，但若没有代码入口、执行顺序、验收命令和回滚边界，直接执行仍会把设计不确定性转化为半完成改动。因此，细节不足时先写计划书，评估通过后才允许执行。

每一轮至少留下以下证据：

1. **可能的下一步**：结合当前代码、规范、测试和发布状态列出候选事项；不得只按愿望或功能想象排序。
2. **自审裁枝**：在提案进入草案前，逐项检查实际调用链、失效模式、设计原则、收益对象、重复性和验证成本，剪去与代码事实不符、收益不足、重复或证据成本不合算的候选；记录保留项、删除项及理由。自审裁枝不等于人工批准，不得代替草案全审。
3. **形成草案/方案**：只把自审后保留的范围带入下一阶段；若方案细节仍不足以支撑行动，再形成计划书。
4. **可执行性评估**：确认目标、代码入口、依赖、顺序、验收标准、回滚边界和授权条件足够明确；评估未通过不得执行。
5. **执行**：只实现已裁决且获得准入的范围；补充仿真/协议/工具测试，并记录真实命令和结果。真实桌面 mutation、破坏性实验、跨机器验证和发布动作仍需明确授权。
6. **全量审查（整体复盘）**：检查代码、规范、文档、测试、供应链、发布边界和挂起事项是否仍自洽；更新 `docs/review-process.md` 与 `AGENTS.project-process.md`，不得以“代码已存在”代替“证据已通过”。

**因为什么事故而加**：此前工作容易在“列出下一步”后直接修改代码，导致 runner 的锁/进程清理、CI 离线依赖、发布清单和真实验证边界分别在不同轮次暴露；没有固定“候选 → 代码裁决 → 执行 → 整体复盘”，很难区分已验证事实、局部修复和仍然挂起的项目级结论。
